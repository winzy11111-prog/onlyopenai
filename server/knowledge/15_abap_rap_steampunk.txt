ABAP RAP (RESTful ABAP Programming) & STEAMPUNK — PetabyteAi KB
================================================================
Version: S/4HANA 2023 / ABAP Cloud 2024

## OVERVIEW

RAP = RESTful ABAP Programming Model. It is the successor to BOPF /
Classic Workflow / CHIP / SADL. Used for:
- Fiori Elements apps (list, OP, analytical, overview)
- OData V2 & V4 services
- ABAP Cloud (Steampunk) and on-prem S/4HANA (2020+)

RAP has two flavors:
1. **Managed** — framework generates persistence (DB tables auto-updated)
2. **Unmanaged** — developer writes save/update/load logic (wraps legacy BAPIs)

Plus a hybrid: **Managed with unmanaged save** (common for legacy reuse).

## RAP ARCHITECTURE LAYERS

```
┌───────────────────────────────────┐
│   Service Binding (OData V2/V4)    │  ← published endpoint
├───────────────────────────────────┤
│   Service Definition (EXPOSE)      │  ← which entities/actions
├───────────────────────────────────┤
│   Projection View (CDS)            │  ← UI-level, aliasing
├───────────────────────────────────┤
│   Metadata Extension (MDE)         │  ← Fiori annotations (UI.*)
├───────────────────────────────────┤
│   Behavior Projection              │  ← UI-specific: use actions
├───────────────────────────────────┤
│   Root CDS View Entity + BDEF      │  ← the BO
├───────────────────────────────────┤
│   Behavior Implementation (ABP)    │  ← ABAP class: global+local
├───────────────────────────────────┤
│   Database Table (managed) /       │
│   Legacy BAPI  (unmanaged)          │
└───────────────────────────────────┘
```

## NAMING CONVENTIONS

| Artifact | Suggested |
|----------|-----------|
| Root CDS entity | `ZR_<BO>` (e.g. ZR_TRAVEL) |
| Projection CDS  | `ZC_<BO>` (consumption) |
| Behavior Def    | same name as root CDS |
| Behavior Imp class | `ZBP_R_<BO>` (handler) |
| Service Def     | `ZUI_<BO>` or `ZAPI_<BO>` |
| Service Binding | `ZUI_<BO>_O4` (O2=OData V2, O4=V4) |

## ROOT CDS VIEW ENTITY

```abap
@AccessControl.authorizationCheck: #CHECK
@EndUserText.label: 'Travel Root'
@Metadata.allowExtensions: true
define root view entity ZR_Travel
  as select from /dmo/travel
  composition [0..*] of ZR_Booking as _Booking
  association [1..1] to /dmo/agency as _Agency on $projection.AgencyID = _Agency.agency_id
{
  key travel_id       as TravelID,
      agency_id       as AgencyID,
      customer_id     as CustomerID,
      @Semantics.amount.currencyCode: 'CurrencyCode'
      booking_fee     as BookingFee,
      total_price     as TotalPrice,
      currency_code   as CurrencyCode,
      overall_status  as OverallStatus,
      @Semantics.user.createdBy: true
      created_by      as CreatedBy,
      @Semantics.systemDateTime.createdAt: true
      created_at      as CreatedAt,
      @Semantics.user.lastChangedBy: true
      last_changed_by as LastChangedBy,
      @Semantics.systemDateTime.lastChangedAt: true
      last_changed_at as LastChangedAt,
      _Booking,
      _Agency
}
```

## BEHAVIOR DEFINITION (BDEF) — MANAGED

```abap
managed implementation in class zbp_r_travel unique;
strict ( 2 );

define behavior for ZR_Travel alias Travel
persistent table /dmo/travel
lock master
authorization master ( instance )
etag master LastChangedAt
{
  create;
  update;
  delete;
  association _Booking { create; with draft; }

  field ( readonly ) TravelID, CreatedBy, CreatedAt, LastChangedBy, LastChangedAt;
  field ( mandatory ) AgencyID, CustomerID;

  action ( features : instance ) acceptTravel result [1] $self;
  action ( features : instance ) rejectTravel result [1] $self;

  determination setInitialStatus on modify { create; }
  validation validateCustomer on save { create; field CustomerID; }

  mapping for /dmo/travel {
    TravelID       = travel_id;
    AgencyID       = agency_id;
    CustomerID     = customer_id;
    BookingFee     = booking_fee;
    TotalPrice     = total_price;
    CurrencyCode   = currency_code;
    OverallStatus  = overall_status;
    CreatedBy      = created_by;
    CreatedAt      = created_at;
    LastChangedBy  = last_changed_by;
    LastChangedAt  = last_changed_at;
  }
}
```

## BEHAVIOR IMPLEMENTATION — handlers

```abap
CLASS lhc_travel DEFINITION INHERITING FROM cl_abap_behavior_handler.

  PRIVATE SECTION.

    METHODS:
      get_instance_features   FOR INSTANCE FEATURES
                              IMPORTING keys REQUEST requested_features FOR Travel
                              RESULT    result,

      acceptTravel            FOR MODIFY
                              IMPORTING keys FOR ACTION Travel~acceptTravel
                              RESULT    result,

      setInitialStatus        FOR DETERMINE ON MODIFY
                              IMPORTING keys FOR Travel~setInitialStatus,

      validateCustomer        FOR VALIDATE ON SAVE
                              IMPORTING keys FOR Travel~validateCustomer.

ENDCLASS.

CLASS lhc_travel IMPLEMENTATION.

  METHOD acceptTravel.
    MODIFY ENTITIES OF zr_travel IN LOCAL MODE
      ENTITY Travel
        UPDATE FIELDS ( OverallStatus )
        WITH VALUE #( FOR key IN keys ( %tky = key-%tky OverallStatus = 'A' ) )
      FAILED failed
      REPORTED reported.

    READ ENTITIES OF zr_travel IN LOCAL MODE
      ENTITY Travel ALL FIELDS WITH
        VALUE #( FOR key IN keys ( %tky = key-%tky ) )
      RESULT DATA(travels).

    result = VALUE #( FOR travel IN travels
                      ( %tky   = travel-%tky
                        %param = travel ) ).
  ENDMETHOD.

  METHOD setInitialStatus.
    MODIFY ENTITIES OF zr_travel IN LOCAL MODE
      ENTITY Travel
        UPDATE FIELDS ( OverallStatus )
        WITH VALUE #( FOR key IN keys ( %tky = key-%tky OverallStatus = 'O' ) )
      REPORTED reported.
  ENDMETHOD.

  METHOD validateCustomer.
    READ ENTITIES OF zr_travel IN LOCAL MODE
      ENTITY Travel FIELDS ( CustomerID )
        WITH VALUE #( FOR key IN keys ( %tky = key-%tky ) )
      RESULT DATA(travels).

    LOOP AT travels INTO DATA(t).
      SELECT SINGLE customer_id FROM /dmo/customer
        WHERE customer_id = @t-CustomerID
        INTO @DATA(lv_exists).
      IF sy-subrc <> 0.
        APPEND VALUE #( %tky = t-%tky ) TO failed-travel.
        APPEND VALUE #( %tky = t-%tky
                        %msg = new_message_with_text(
                          severity = if_abap_behv_message=>severity-error
                          text     = |Customer { t-CustomerID } not found| )
                        %element-CustomerID = if_abap_behv=>mk-on )
               TO reported-travel.
      ENDIF.
    ENDLOOP.
  ENDMETHOD.

ENDCLASS.
```

## PROJECTION + METADATA EXTENSION (UI)

### Projection CDS
```abap
@AccessControl.authorizationCheck: #CHECK
@EndUserText.label: 'Travel Projection'
@Metadata.allowExtensions: true
@Search.searchable: true
define root view entity ZC_Travel
  provider contract transactional_query
  as projection on ZR_Travel
{
  key TravelID,
      @Search.defaultSearchElement: true
      AgencyID,
      @Search.defaultSearchElement: true
      CustomerID,
      BookingFee,
      TotalPrice,
      CurrencyCode,
      OverallStatus,
      _Agency,
      _Booking : redirected to composition child ZC_Booking
}
```

### Metadata Extension (Fiori UI annotations)
```abap
@Metadata.layer: #CORE
annotate entity ZC_Travel with
{
  @UI.headerInfo: { typeName: 'Travel', typeNamePlural: 'Travels', title: { value: 'TravelID' } }

  @UI.lineItem:       [{ position: 10, label: 'ID' }]
  @UI.selectionField: [{ position: 10 }]
  TravelID;

  @UI.lineItem:       [{ position: 20, label: 'Agency' }]
  @UI.selectionField: [{ position: 20 }]
  AgencyID;

  @UI.lineItem:       [{ position: 30 }]
  CustomerID;

  @UI.lineItem:       [{ position: 40 }]
  TotalPrice;

  @UI.lineItem:       [{ position: 50, criticality: 'OverallStatusCriticality' }]
  OverallStatus;
}
```

## SERVICE DEFINITION & BINDING

```abap
" Service Definition: ZUI_TRAVEL
@EndUserText.label: 'UI Service for Travel'
define service ZUI_TRAVEL {
  expose ZC_Travel  as Travel;
  expose ZC_Booking as Booking;
}
```

Service Binding → right-click → Create → pick scenario:
- **Fiori Elements OData V4** — for List Report / Object Page
- **Web API OData V4** — for external REST consumers
- **OData V2** — legacy (pre-1909 on-prem)

## RAP FEATURES — control actions dynamically

```abap
METHOD get_instance_features.
  READ ENTITIES OF zr_travel IN LOCAL MODE
    ENTITY Travel FIELDS ( OverallStatus )
      WITH CORRESPONDING #( keys )
    RESULT DATA(travels).

  result = VALUE #( FOR t IN travels
    ( %tky                      = t-%tky
      %action-acceptTravel      = COND #(
        WHEN t-OverallStatus = 'A' THEN if_abap_behv=>fc-o-disabled
        ELSE if_abap_behv=>fc-o-enabled )
      %action-rejectTravel      = COND #(
        WHEN t-OverallStatus = 'X' THEN if_abap_behv=>fc-o-disabled
        ELSE if_abap_behv=>fc-o-enabled ) ) ).
ENDMETHOD.
```

## DRAFTS

```abap
" In BDEF:
managed implementation in class zbp_r_travel unique;
with draft;

define behavior for ZR_Travel alias Travel
persistent table /dmo/travel
draft table zdraft_travel
lock master total etag LastChangedAt
authorization master ( instance )
etag master LastChangedAt
{
  ... same as before ...
  draft action Activate;
  draft action Edit;
  draft action Discard;
  draft action Resume;
  draft determine action Prepare { validation validateCustomer; }
}
```

## UNMANAGED BEHAVIOR — wrapping legacy BAPI

```abap
unmanaged implementation in class zbp_r_travel_unmanaged unique;

define behavior for ZR_Travel alias Travel
late numbering
lock master total etag LastChangedAt
{
  create;  update;  delete;
  mapping for /dmo/travel corresponding;
}

" In handler class — implement every CRUD method yourself:
" METHOD create.  CALL FUNCTION 'BAPI_TRAVEL_CREATE' ...  ENDMETHOD.
" METHOD save.    CALL FUNCTION 'BAPI_TRANSACTION_COMMIT' ... ENDMETHOD.
```

## TESTING RAP

### EML (Entity Manipulation Language) in unit test
```abap
MODIFY ENTITIES OF zr_travel
  ENTITY Travel
    CREATE FIELDS ( AgencyID CustomerID BookingFee CurrencyCode )
    WITH VALUE #( ( %cid = 'C1' AgencyID = '070001' CustomerID = '1' BookingFee = 10 CurrencyCode = 'USD' ) )
  MAPPED DATA(mapped)
  FAILED DATA(failed)
  REPORTED DATA(reported).

COMMIT ENTITIES RESPONSES FAILED FAILED RESPONSES REPORTED reported.
```

### RAP Unit Test (CL_CDS_TEST_ENVIRONMENT)
```abap
CLASS ltc_travel DEFINITION FOR TESTING RISK LEVEL HARMLESS DURATION SHORT.
  PRIVATE SECTION.
    CLASS-DATA env TYPE REF TO if_cds_test_environment.
    CLASS-METHODS class_setup.
    CLASS-METHODS class_teardown.
    METHODS setup.
    METHODS test_create FOR TESTING.
ENDCLASS.
```

## STEAMPUNK (ABAP ENVIRONMENT ON BTP) — CONSTRAINTS

- **Released APIs only** — ABAP language version = ABAP for Cloud Development.
- **No CALL FUNCTION to non-released RFC**. Whitelist via C1/released objects.
- **No direct SELECT on SAP tables** — use released CDS views instead.
- **No open SQL on MATDOC/MARA etc. directly** — use I_Product, I_Material.
- Output = **Communication Arrangements** (SCC on BTP).
- **No dynpro / module pool** — only RAP-based Fiori Elements.
- Tests run in **isolated client** with CDS Test Doubles.

## PREDELIVERED CDS VIEWS TO KNOW

| View | Purpose |
|------|---------|
| I_Product | Generic product (material) master |
| I_BusinessPartner | BP (includes customers/vendors) |
| I_PurchaseOrder | PO header |
| I_PurchaseOrderItem | PO line |
| I_SalesOrder | SO header |
| I_Material, I_MaterialText | Material attributes |
| I_JournalEntryItem | FI journal |
| I_CompanyCode | Org unit |

## MIGRATION HINT — BOPF → RAP

BOPF business objects from pre-1909 can be re-wrapped as unmanaged RAP.
Use **CL_BOBF_RAP_SUPERCLASS** as handler base and expose via BDEF. New
development should **never** start in BOPF.
